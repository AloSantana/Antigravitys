# Root-level dependencies (dev tools + packages not in backend/requirements.txt)
# NOTE: google-genai, google-cloud-aiplatform, pydantic, pydantic-settings,
# python-dotenv, and pyngrok are already in backend/requirements.txt.
# The Dockerfile installs BOTH files, so duplicates are harmless but redundant.
pytest
pytest-cov
pytest-asyncio
httpx
mcp
